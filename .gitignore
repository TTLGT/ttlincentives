node_modules
dist
dist-ssr
*.local
.DS_Store

# Nunca subir credenciales de servidor al repositorio.
*serviceAccount*.json
*service-account*.json
firebase-adminsdk*.json
.env
.env.local
.env.*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Fotos de los brokers: se quedan LOCALES.
# Se suben a Firestore con el seed, dentro del documento de cada broker, para
# que las protejan las reglas. Nunca al repositorio: es publico, y lo que se
# publica en GitHub Pages lo baja cualquiera sin iniciar sesion.
photos-source/
